import styled from "styled-components"

export const MenuLayout = styled.nav`
  ul {
    list-style: none;
    margin: 0;
    padding: 0;
  }

  li {
    color: #fff;
    display: inline-flex;
    margin-right: 12px;
  }
`
